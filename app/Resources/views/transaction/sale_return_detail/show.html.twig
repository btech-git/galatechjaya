{% extends 'transaction/layout.html.twig' %}

{% block title 'Show Sale Return Detail' %}

{% block actions %}
    {{ include('transaction/actions.html.twig') }}
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Quantity</th>
                <td>{{ saleReturnDetail.quantity|default }}</td>
            </tr>
            <tr>
                <th>Unit Price</th>
                <td>{{ saleReturnDetail.unitPrice|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ saleReturnDetail.total|default|number_format(2) }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
