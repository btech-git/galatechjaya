{% extends 'transaction/layout.html.twig' %}

{% block title 'Show Sale Payment Header' %}

{% block actions %}
    {{ include('transaction/actions.html.twig') }}
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Transaction Date</th>
                <td>{{ salePaymentHeader.transactionDate|default ? salePaymentHeader.transactionDate|date('Y-m-d') }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ salePaymentHeader.note|default }}</td>
            </tr>
            <tr>
                <th>Total Amount</th>
                <td>{{ salePaymentHeader.totalAmount|default|number_format(2) }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
