{% extends 'transaction/layout.html.twig' %}

{% block title 'Show Sale Invoice Detail' %}

{% block actions %}
    {{ include('transaction/actions.html.twig') }}
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Quantity</th>
                <td>{{ saleInvoiceDetail.quantity|default }}</td>
            </tr>
            <tr>
                <th>Unit Price</th>
                <td>{{ saleInvoiceDetail.unitPrice|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Discount</th>
                <td>{{ saleInvoiceDetail.discount|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ saleInvoiceDetail.total|default }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
