{% set grid_layout = 'datagrid.html.twig' %}

{{ grid_top(grid) }}

<table data-role="table" class="table table-bordered table-striped table-condensed table-hover">
    <thead>
        <tr data-action="sort" class="alert alert-info">
            <th data-group="saleInvoiceHeader" data-field="transactionDate">Transaction Date</th>
            <th data-group="saleInvoiceHeader" data-field="taxInvoiceCode">Tax Invoice Code</th>
            <th data-group="saleInvoiceHeader" data-field="customerInvoice">Customer Invoice</th>
            <th data-group="saleInvoiceHeader" data-field="subTotal">Sub Total</th>
            <th data-group="saleInvoiceHeader" data-field="discount">Discount</th>
            <th data-group="saleInvoiceHeader" data-field="taxNominal">Tax Nominal</th>
            <th data-group="saleInvoiceHeader" data-field="shippingFee">Shipping Fee</th>
            <th data-group="saleInvoiceHeader" data-field="totalReturn">Total Return</th>
            <th data-group="saleInvoiceHeader" data-field="grandTotal">Grand Total</th>
            <th data-group="saleInvoiceHeader" data-field="note">Note</th>
            <th data-group="saleInvoiceHeader" data-field="isTax">Is Tax</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {% for saleInvoiceHeader in grid.data %}
            <tr data-field-id="{{ saleInvoiceHeader.id }}">
                <td class="text-left">{{ saleInvoiceHeader.transactionDate|default ? saleInvoiceHeader.transactionDate|date('Y-m-d') }}</td>
                <td class="text-left">{{ saleInvoiceHeader.taxInvoiceCode|default }}</td>
                <td class="text-left">{{ saleInvoiceHeader.customerInvoice|default }}</td>
                <td class="text-right">{{ saleInvoiceHeader.subTotal|default|number_format(2) }}</td>
                <td class="text-right">{{ saleInvoiceHeader.discount|default|number_format(2) }}</td>
                <td class="text-right">{{ saleInvoiceHeader.taxNominal|default|number_format(2) }}</td>
                <td class="text-right">{{ saleInvoiceHeader.shippingFee|default|number_format(2) }}</td>
                <td class="text-right">{{ saleInvoiceHeader.totalReturn|default|number_format(2) }}</td>
                <td class="text-right">{{ saleInvoiceHeader.grandTotal|default|number_format(2) }}</td>
                <td class="text-left">{{ saleInvoiceHeader.note|default }}</td>
                <td class="text-left">{{ saleInvoiceHeader.isTax|default ? 'Yes' : 'No' }}</td>
                <td class="text-left">
                    {{ include('transaction/actions.html.twig', {
                        'contents': {
                            '{index}': null,
                            '{new}': null,
                            '{show}': include('action.html.twig', {'url': path('transaction_sale_invoice_header_show', {'id': saleInvoiceHeader.id}), 'button': 'btn btn-info btn-sm', 'icon': 'glyphicon glyphicon-info-sign'}),
                            '{edit}': include('action.html.twig', {'url': path('transaction_sale_invoice_header_edit', {'id': saleInvoiceHeader.id}), 'button': 'btn btn-warning btn-sm', 'icon': 'glyphicon glyphicon-edit'}),
                            '{delete}': include('action.html.twig', {'url': path('transaction_sale_invoice_header_delete', {'id': saleInvoiceHeader.id}), 'button': 'btn btn-danger btn-sm', 'icon': 'glyphicon glyphicon-remove'}),
                        }
                    }) }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12" class="alert alert-warning">
                    <strong>There are no items to display.</strong>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{{ grid_bottom(grid) }}
