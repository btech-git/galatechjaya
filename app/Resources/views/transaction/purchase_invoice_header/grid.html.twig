{% set grid_layout = 'datagrid.html.twig' %}

{{ grid_top(grid) }}

<table data-role="table" class="table table-bordered table-striped table-condensed table-hover">
    <thead>
        <tr data-action="sort" class="alert alert-info">
            <th data-group="purchaseInvoiceHeader" data-field="transactionDate">Transaction Date</th>
            <th data-group="purchaseInvoiceHeader" data-field="taxInvoiceCode">Tax Invoice Code</th>
            <th data-group="purchaseInvoiceHeader" data-field="supplierInvoice">Supplier Invoice</th>
            <th data-group="purchaseInvoiceHeader" data-field="subTotal">Sub Total</th>
            <th data-group="purchaseInvoiceHeader" data-field="discount">Discount</th>
            <th data-group="purchaseInvoiceHeader" data-field="taxNominal">Tax Nominal</th>
            <th data-group="purchaseInvoiceHeader" data-field="shippingFee">Shipping Fee</th>
            <th data-group="purchaseInvoiceHeader" data-field="totalReturn">Total Return</th>
            <th data-group="purchaseInvoiceHeader" data-field="grandTotal">Grand Total</th>
            <th data-group="purchaseInvoiceHeader" data-field="note">Note</th>
            <th data-group="purchaseInvoiceHeader" data-field="isTax">Is Tax</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {% for purchaseInvoiceHeader in grid.data %}
            <tr data-field-id="{{ purchaseInvoiceHeader.id }}">
                <td class="text-left">{{ purchaseInvoiceHeader.transactionDate|default ? purchaseInvoiceHeader.transactionDate|date('Y-m-d') }}</td>
                <td class="text-left">{{ purchaseInvoiceHeader.taxInvoiceCode|default }}</td>
                <td class="text-left">{{ purchaseInvoiceHeader.supplierInvoice|default }}</td>
                <td class="text-right">{{ purchaseInvoiceHeader.subTotal|default|number_format(2) }}</td>
                <td class="text-right">{{ purchaseInvoiceHeader.discount|default|number_format(2) }}</td>
                <td class="text-right">{{ purchaseInvoiceHeader.taxNominal|default|number_format(2) }}</td>
                <td class="text-right">{{ purchaseInvoiceHeader.shippingFee|default|number_format(2) }}</td>
                <td class="text-right">{{ purchaseInvoiceHeader.totalReturn|default|number_format(2) }}</td>
                <td class="text-right">{{ purchaseInvoiceHeader.grandTotal|default|number_format(2) }}</td>
                <td class="text-left">{{ purchaseInvoiceHeader.note|default }}</td>
                <td class="text-left">{{ purchaseInvoiceHeader.isTax|default ? 'Yes' : 'No' }}</td>
                <td class="text-left">
                    {{ include('transaction/actions.html.twig', {
                        'contents': {
                            '{index}': null,
                            '{new}': null,
                            '{show}': include('action.html.twig', {'url': path('transaction_purchase_invoice_header_show', {'id': purchaseInvoiceHeader.id}), 'button': 'btn btn-info btn-sm', 'icon': 'glyphicon glyphicon-info-sign'}),
                            '{edit}': include('action.html.twig', {'url': path('transaction_purchase_invoice_header_edit', {'id': purchaseInvoiceHeader.id}), 'button': 'btn btn-warning btn-sm', 'icon': 'glyphicon glyphicon-edit'}),
                            '{delete}': include('action.html.twig', {'url': path('transaction_purchase_invoice_header_delete', {'id': purchaseInvoiceHeader.id}), 'button': 'btn btn-danger btn-sm', 'icon': 'glyphicon glyphicon-remove'}),
                        }
                    }) }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12" class="alert alert-warning">
                    <strong>There are no items to display.</strong>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{{ grid_bottom(grid) }}
