{% extends 'transaction/layout.html.twig' %}

{% block title 'Show Sale Order Header' %}

{% block actions %}
    {{ include('transaction/actions.html.twig') }}
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Transaction Date</th>
                <td>{{ saleOrderHeader.transactionDate|default ? saleOrderHeader.transactionDate|date('Y-m-d') }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ saleOrderHeader.note|default }}</td>
            </tr>
            <tr>
                <th>Sub Total</th>
                <td>{{ saleOrderHeader.subTotal|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Discount</th>
                <td>{{ saleOrderHeader.discount|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Tax Nominal</th>
                <td>{{ saleOrderHeader.taxNominal|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Shipping Fee</th>
                <td>{{ saleOrderHeader.shippingFee|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Grand Total</th>
                <td>{{ saleOrderHeader.grandTotal|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Is Tax</th>
                <td>{{ saleOrderHeader.isTax|default ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
